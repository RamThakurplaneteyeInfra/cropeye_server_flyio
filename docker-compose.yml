services:
  # Local database service - commented out as we're using hosted database
  # db:
  #   image: postgis/postgis:15-3.3
  #   container_name: cropeye_postgres
  #   environment:
  #     POSTGRES_DB: ${DB_NAME:-neoce}
  #     POSTGRES_USER: ${DB_USER:-postgres}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-admin}
  #     POSTGRES_INITDB_ARGS: "-E UTF8 --locale=en_US.UTF-8"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "${DB_PORT:-5432}:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d neoce || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 30s
  #   restart: unless-stopped
  #   networks:
  #     - cropeye_network

  web:
    build: .
    container_name: cropeye_web
    ports:
      - "8000:8000" # Maps host port 8000 to container port 8000 (binds to all interfaces by default)
    volumes:
      - .:/app # Mount the whole directory
    env_file:
      - .env
    environment:
      # Hosted database configuration
      - DB_HOST=dev-et.cropeye.ai
      - DB_PORT=5432
      - DB_NAME=CROPDB_TEST
      - DB_USER=farm_management_l1wj_user
      - DB_PASSWORD=DySO3fcTFjb8Rgp9IZIxGYgLZ7KxwmjL
      - ALLOWED_HOSTS=* # Allow all hosts for local network access
      - DEBUG=True # Enable DEBUG for local development
    # Removed depends_on since we're using hosted database, not local db service
    # depends_on:
    #   db:
    #     condition: service_healthy
    restart: unless-stopped
    networks:
      - cropeye_network

# Local database volume - not needed when using hosted database
# volumes:
#   postgres_data:
#     driver: local

networks:
  cropeye_network:
    driver: bridge
